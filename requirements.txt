# PyTorch and Core AI
# Ghim đúng version đã verify chạy ổn định (Mac dev + GPU cloud cả session) —
# base image Docker (pytorch/pytorch:2.1.0-cuda12.1-cudnn8-runtime) cố định
# sẵn torch 2.1.0, và "torch>=2.0.0" (không giới hạn trên) không ép pip nâng
# cấp vì 2.1.0 đã thoả điều kiện. Trong khi đó "diffusers>=0.25.0" luôn cài
# bản mới nhất — bản mới này gọi torch.xpu (chỉ có từ torch ~2.4+) ngay lúc
# import module, torch 2.1.0 không có attribute đó → AttributeError treo
# ngay khi container start, tốn công GPU rebuild nhiều lần mới lộ ra.
torch==2.12.0
torchvision==0.27.0
diffusers==0.38.0
transformers>=4.36.0
accelerate>=0.26.0
peft>=0.10.0
compel>=2.0.0

# gRPC and APIs
grpcio>=1.59.0
grpcio-tools>=1.59.0
protobuf>=4.25.0
fastapi>=0.100.0
uvicorn>=0.22.0
pydantic-settings>=2.0.0

# Storage & Caching
minio>=7.2.0
redis>=5.0.0
celery>=5.3.0

# Image Processing
Pillow>=10.0.0

# Monitoring
prometheus-client>=0.19.0

# Utilities & Dev
python-dotenv>=1.0.0
pytest>=7.4.0
uuid6>=2024.1.12
requests>=2.31.0


# Data validation
pydantic>=2.5.0
pydantic-settings>=2.1.0
